{
  "name": "sys-cli",
  "version": "1.0.0",
  "description": "CLI para generación de código desde JSONs",
  "scripts": {
    "sync": "npx tsx sync.ts",
    "sync:migrate": "npx tsx sync.ts --migrate",
    "generate": "npx tsx generate-sqlmodel.ts",
    "seed": "npx tsx seed.ts",
    "seed:hotel": "npx tsx seed-hotel.ts",
    "seed:menu": "npx tsx seed-menu.ts",
    "clean-db": "npx tsx clean-db.ts"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "puppeteer": "^24.35.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^17.2.3",
    "mysql2": "^3.16.1"
  }
}
